# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azure-sql-db-session-recommender
metadata:
  template: azure-sql-db-session-recommender
services:
  web:
    project: ./client
    language: js
    host: staticwebapp
    dist: dist
  functionapp:
    project: ./func
    language: dotnet
    host: function